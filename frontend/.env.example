# Backend URL (Cloudflare Worker)
# Used for both REST API (http/https) and WebSocket (ws/wss) connections
# The frontend automatically converts ws:// to http:// for REST API calls
VITE_BACKEND_URL=ws://localhost:8787
