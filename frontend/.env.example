# Backend API URL (Cloudflare Worker)
# For local development, point to the local Cloudflare Workers dev server
VITE_BACKEND_URL=http://localhost:8787

# For production, point to your deployed Cloudflare Workers URL
# VITE_BACKEND_URL=https://your-worker.your-subdomain.workers.dev
